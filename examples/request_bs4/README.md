# Requests + BeautifulSoup Example

This script scrapes quotes and authors from a sample website.  
It demonstrates how to:
- Make HTTP requests with `requests`
- Parse HTML content with `BeautifulSoup`
- Extract specific data using HTML tags and classes